{
  "name": "roles-platform-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo with Next.js (Tailwind + role-focused UI), Node.js TypeScript API, and PostgreSQL.",
  "scripts": {
    "dev:web": "pnpm --dir apps/web dev",
    "dev:api": "pnpm --dir apps/api dev",
    "dev": "pnpm run dev:web & pnpm run dev:api",
    "build": "pnpm run build:web && pnpm run build:api",
    "build:web": "pnpm --dir apps/web build",
    "build:api": "pnpm --dir apps/api build",
    "lint": "pnpm --dir apps/web lint && pnpm --dir apps/api lint",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "db:migrate": "pnpm --dir apps/api prisma migrate dev",
    "db:seed": "pnpm --dir apps/api prisma db seed"
  },
  "author": "generated by Hermes",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
